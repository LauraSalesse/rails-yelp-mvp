<h1>Add a review for restaurant </h1>
<%=  simple_form_for([@restaurant, @review]) do |r| %>
  <%= r.error_notification %>

  <%= r.input :content %>
  <%= r.input :rating %>

  <%= r.submit%>
<% end %>
