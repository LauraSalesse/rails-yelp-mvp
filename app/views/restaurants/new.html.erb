<h1>New restaurant:</h1>

<%= simple_form_for(@restaurant) do |r| %>
  <%= r.error_notification %>

  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :category, collection: ["chinese", "italian", "japanese", "french", "belgian"], prompt: "Choose one!" %>

  <%= r.submit%>
<% end %>
